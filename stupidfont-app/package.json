{
  "name": "stupidfont-app",
  "version": "1.0.0",
  "private": true,
  "description": "Native shells (iOS + Android) for stupidfont, the finger-drawn font editor at noun.wtf/stupidfont. Web code lives in packages/nouns-webapp/public/stupidfont.",
  "scripts": {
    "sync": "cap sync",
    "ios": "cap open ios",
    "android": "cap open android",
    "build:android": "cap sync android && cd android && ./gradlew bundleRelease assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/cli": "^8.5.2",
    "@capacitor/core": "^8.5.2",
    "@capacitor/filesystem": "^8.1.3",
    "@capacitor/ios": "^8.5.2",
    "@capacitor/share": "^8.0.1"
  }
}
