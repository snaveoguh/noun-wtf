/**
 * Thin wrapper that re-exports EditableScene from @nouns/voxel-engine.
 * Exists as a separate file so NounParallax can lazy-load it.
 */
export { default } from '@nouns/voxel-engine/src/react/EditableScene';
