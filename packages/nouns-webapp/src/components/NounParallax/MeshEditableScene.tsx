/**
 * Thin wrapper that re-exports EditableMeshScene from @nouns/mesh-editor.
 * Exists as a separate file so NounParallax can lazy-load it.
 */
export { default } from '@nouns/mesh-editor/src/react/EditableMeshScene';
