{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "pnpm --filter @nouns/api -- start --schema nouns-prod-v2",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  }
}
